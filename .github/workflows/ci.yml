name: "ci"

on:
  pull_request:
    branches: ["main"]

permissions:
  contents: read
  checks: write
  id-token: write

env:
  COPILOT_AGENT_FIREWALL_ALLOW_LIST_ADDITIONS: "playwright.dev,storybook.js.org"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 2

      - name: Cache turbo build setup
        uses: actions/cache@v6
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Setup node
        uses: actions/setup-node@v7
        with:
          node-version: "24"

      - name: Install pnpm
        uses: pnpm/action-setup@v6.0.9
        id: pnpm-install
        with:
          run_install: false

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - uses: actions/cache@v6
        name: Setup pnpm cache
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Lint application
        run: pnpm lint

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 2

      - name: Cache turbo build setup
        uses: actions/cache@v6
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Setup node
        uses: actions/setup-node@v7
        with:
          node-version: "24"

      - name: Install pnpm
        uses: pnpm/action-setup@v6.0.9
        id: pnpm-install
        with:
          run_install: false

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - uses: actions/cache@v6
        name: Setup pnpm cache
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Build application
        run: pnpm build

  test-components:
    name: Test components
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 2

      - name: Cache turbo build setup
        uses: actions/cache@v6
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Setup node
        uses: actions/setup-node@v7
        with:
          node-version: "24"

      - name: Install pnpm
        uses: pnpm/action-setup@v6.0.9
        id: pnpm-install
        with:
          run_install: false

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Setup pnpm cache
        uses: actions/cache@v6
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Get installed Playwright version
        id: playwright-version
        run: echo "PLAYWRIGHT_VERSION=$(grep '^    playwright:' pnpm-workspace.yaml | awk '{print $2}')" >> $GITHUB_OUTPUT

      - name: Cache playwright binaries
        uses: actions/cache@v6
        id: playwright-cache
        with:
          path: |
            ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ steps.playwright-version.outputs.PLAYWRIGHT_VERSION }}

      - name: Install Playwright browsers
        run: pnpm --filter=e2e exec playwright install chromium
        if: steps.playwright-cache.outputs.cache-hit != 'true'

      - name: Cache Playwright system dependencies
        id: apt-cache-components
        uses: actions/cache@v6
        with:
          path: ~/.cache/playwright-debs
          key: ${{ runner.os }}-apt-playwright-${{ steps.playwright-version.outputs.PLAYWRIGHT_VERSION }}

      - name: Install Playwright system dependencies
        if: steps.apt-cache-components.outputs.cache-hit != 'true'
        run: pnpm --filter=e2e exec playwright install-deps chromium

      - name: Collect apt debs for next run
        if: steps.apt-cache-components.outputs.cache-hit != 'true'
        run: |
          mkdir -p ~/.cache/playwright-debs
          sudo cp /var/cache/apt/archives/*.deb ~/.cache/playwright-debs/ 2>/dev/null || true

      - name: Run tests
        run: pnpm test:ci
        continue-on-error: false

      - name: Publish test summary
        uses: mikepenz/action-junit-report@v6
        with:
          report_paths: "**/.test-results/*.xml"
          check_name: "Test components summary"
        if: always()

  test-e2e:
    name: Test E2E
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 2

      - name: Cache turbo build setup
        uses: actions/cache@v6
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Setup node
        uses: actions/setup-node@v7
        with:
          node-version: "24"

      - name: Install pnpm
        uses: pnpm/action-setup@v6.0.9
        id: pnpm-install
        with:
          run_install: false

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Setup pnpm cache
        uses: actions/cache@v6
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Get installed Playwright version
        id: playwright-version
        run: echo "PLAYWRIGHT_VERSION=$(grep '^    playwright:' pnpm-workspace.yaml | awk '{print $2}')" >> $GITHUB_OUTPUT

      - name: Cache playwright binaries
        uses: actions/cache@v6
        id: playwright-cache
        with:
          path: |
            ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ steps.playwright-version.outputs.PLAYWRIGHT_VERSION }}

      - name: Install Playwright browsers
        run: pnpm --filter=e2e exec playwright install chromium
        if: steps.playwright-cache.outputs.cache-hit != 'true'

      - name: Cache Playwright system dependencies
        id: apt-cache-e2e
        uses: actions/cache@v6
        with:
          path: ~/.cache/playwright-debs
          key: ${{ runner.os }}-apt-playwright-${{ steps.playwright-version.outputs.PLAYWRIGHT_VERSION }}

      - name: Install Playwright system dependencies
        if: steps.apt-cache-e2e.outputs.cache-hit != 'true'
        run: pnpm --filter=e2e exec playwright install-deps chromium

      - name: Collect apt debs for next run
        if: steps.apt-cache-e2e.outputs.cache-hit != 'true'
        run: |
          mkdir -p ~/.cache/playwright-debs
          sudo cp /var/cache/apt/archives/*.deb ~/.cache/playwright-debs/ 2>/dev/null || true

      - name: Run tests
        run: pnpm test:e2e:ci

      - name: Publish test summary
        uses: mikepenz/action-junit-report@v6
        with:
          report_paths: "**/.test-results/*.xml"
          check_name: "Test E2E summary"
        if: always()

  test-coverage:
    name: Test coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 2

      - name: Cache turbo build setup
        uses: actions/cache@v6
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Setup node
        uses: actions/setup-node@v7
        with:
          node-version: "24"

      - name: Install pnpm
        uses: pnpm/action-setup@v6.0.9
        id: pnpm-install
        with:
          run_install: false

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Setup pnpm cache
        uses: actions/cache@v6
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Get installed Playwright version
        id: playwright-version
        run: echo "PLAYWRIGHT_VERSION=$(grep '^    playwright:' pnpm-workspace.yaml | awk '{print $2}')" >> $GITHUB_OUTPUT

      - name: Cache playwright binaries
        uses: actions/cache@v6
        id: playwright-cache
        with:
          path: |
            ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ steps.playwright-version.outputs.PLAYWRIGHT_VERSION }}

      - name: Install Playwright browsers
        run: pnpm --filter=e2e exec playwright install chromium
        if: steps.playwright-cache.outputs.cache-hit != 'true'

      - name: Cache Playwright system dependencies
        id: apt-cache-coverage
        uses: actions/cache@v6
        with:
          path: ~/.cache/playwright-debs
          key: ${{ runner.os }}-apt-playwright-${{ steps.playwright-version.outputs.PLAYWRIGHT_VERSION }}

      - name: Install Playwright system dependencies
        if: steps.apt-cache-coverage.outputs.cache-hit != 'true'
        run: pnpm --filter=e2e exec playwright install-deps chromium

      - name: Collect apt debs for next run
        if: steps.apt-cache-coverage.outputs.cache-hit != 'true'
        run: |
          mkdir -p ~/.cache/playwright-debs
          sudo cp /var/cache/apt/archives/*.deb ~/.cache/playwright-debs/ 2>/dev/null || true

      - name: Run tests and coverage
        run: pnpm test:coverage:ci
        continue-on-error: false

      - name: Run storybook coverage
        run: pnpm --filter=@app/storybook test:coverage:storybook:ci
        continue-on-error: true
        timeout-minutes: 8

      - name: Run E2E tests with coverage
        run: pnpm --filter=@app/e2e test:e2e:coverage
        continue-on-error: false

      - name: Merge coverage reports
        run: |
          mkdir -p output/coverage
          node tools/merge-coverage.js \
            output/coverage/combined.xml \
            output/coverage/web.xml \
            output/coverage/web-storybook.xml \
            output/coverage/e2e.xml

      - name: Code coverage report
        uses: irongut/CodeCoverageSummary@v1.3.0
        with:
          filename: "output/coverage/combined.xml"
          badge: true
          fail_below_min: true
          format: markdown
          hide_branch_rate: false
          hide_complexity: true
          indicators: true
          output: both
          thresholds: "80 80"

      - name: Update summary with coverage report
        run: |
          cat code-coverage-results.md >> $GITHUB_STEP_SUMMARY

      - name: "Upload coverage report"
        uses: actions/upload-artifact@v7
        with:
          name: code-coverage-results.md
          path: code-coverage-results.md
